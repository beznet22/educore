// AUTO-GENERATED by tools/dispatcher-gen/dispatcher-gen.py
// Source: domains/finance/src/services.rs (137 fns)
//
// Add to domains/finance/src/services.rs and domains/finance/src/lib.rs.
// Template: Wave 192 dispatch_admit_student pattern.

use educore_dispatcher::CommandDispatcher;

impl educore_dispatcher::CommandBounds for CreateWalletCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.wallet.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "wallet" }
}

/// Dispatcher wrapper for [`create_wallet`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_wallet<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateWalletCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(Wallet, WalletCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_wallet::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreditWalletCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.creditWallet.credit" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "credit" }
    fn target_type(&self) -> &'static str { "creditWallet" }
}

/// Dispatcher wrapper for [`credit_wallet`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_credit_wallet<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreditWalletCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(WalletTransaction, WalletCredited)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            credit_wallet::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RequestWalletRefundCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.requestWalletRefund.request" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "request" }
    fn target_type(&self) -> &'static str { "requestWalletRefund" }
}

/// Dispatcher wrapper for [`request_wallet_refund`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_request_wallet_refund<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RequestWalletRefundCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(WalletTransaction, WalletRefundRequested)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            request_wallet_refund::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateWalletTransactionApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.walletTransactionApproval.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "walletTransactionApproval" }
}

/// Dispatcher wrapper for [`create_wallet_transaction_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_wallet_transaction_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateWalletTransactionApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(WalletTransactionApproval, WalletTransactionApprovalCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_wallet_transaction_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ApproveWalletTransactionApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.walletTransactionApproval.approve" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "approve" }
    fn target_type(&self) -> &'static str { "walletTransactionApproval" }
}

/// Dispatcher wrapper for [`approve_wallet_transaction_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_approve_wallet_transaction_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ApproveWalletTransactionApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<WalletTransactionApprovalApproved>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            approve_wallet_transaction_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RejectWalletTransactionApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.walletTransactionApproval.reject" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "reject" }
    fn target_type(&self) -> &'static str { "walletTransactionApproval" }
}

/// Dispatcher wrapper for [`reject_wallet_transaction_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_reject_wallet_transaction_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RejectWalletTransactionApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<WalletTransactionApprovalRejected>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            reject_wallet_transaction_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateExpenseApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseApproval.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "expenseApproval" }
}

/// Dispatcher wrapper for [`create_expense_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_expense_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateExpenseApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealExpenseApproval, ExpenseApprovalCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_expense_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ApproveExpenseApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseApproval.approve" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "approve" }
    fn target_type(&self) -> &'static str { "expenseApproval" }
}

/// Dispatcher wrapper for [`approve_expense_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_approve_expense_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ApproveExpenseApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ExpenseApprovalApproved>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            approve_expense_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RejectExpenseApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseApproval.reject" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "reject" }
    fn target_type(&self) -> &'static str { "expenseApproval" }
}

/// Dispatcher wrapper for [`reject_expense_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_reject_expense_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RejectExpenseApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ExpenseApprovalRejected>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            reject_expense_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateIncomeApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.incomeApproval.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "incomeApproval" }
}

/// Dispatcher wrapper for [`create_income_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_income_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateIncomeApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealIncomeApproval, IncomeApprovalCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_income_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ApproveIncomeApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.incomeApproval.approve" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "approve" }
    fn target_type(&self) -> &'static str { "incomeApproval" }
}

/// Dispatcher wrapper for [`approve_income_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_approve_income_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ApproveIncomeApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<IncomeApprovalApproved>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            approve_income_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RejectIncomeApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.incomeApproval.reject" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "reject" }
    fn target_type(&self) -> &'static str { "incomeApproval" }
}

/// Dispatcher wrapper for [`reject_income_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_reject_income_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RejectIncomeApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<IncomeApprovalRejected>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            reject_income_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreatePayrollPaymentApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.payrollPaymentApproval.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "payrollPaymentApproval" }
}

/// Dispatcher wrapper for [`create_payroll_payment_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_payroll_payment_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreatePayrollPaymentApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(PayrollPaymentApproval, PayrollPaymentApprovalCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_payroll_payment_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ApprovePayrollPaymentApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.payrollPaymentApproval.approve" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "approve" }
    fn target_type(&self) -> &'static str { "payrollPaymentApproval" }
}

/// Dispatcher wrapper for [`approve_payroll_payment_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_approve_payroll_payment_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ApprovePayrollPaymentApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<PayrollPaymentApprovalApproved>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            approve_payroll_payment_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RejectPayrollPaymentApprovalCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.payrollPaymentApproval.reject" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "reject" }
    fn target_type(&self) -> &'static str { "payrollPaymentApproval" }
}

/// Dispatcher wrapper for [`reject_payroll_payment_approval`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_reject_payroll_payment_approval<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RejectPayrollPaymentApprovalCommand,
    clock: &C,
    ids: &G
) -> Result<Result<PayrollPaymentApprovalRejected>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            reject_payroll_payment_approval::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesDiscount.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesDiscount" }
}

/// Dispatcher wrapper for [`create_fees_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesDiscount, FeesDiscountCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RecordPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.recordPayment.record" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "record" }
    fn target_type(&self) -> &'static str { "recordPayment" }
}

/// Dispatcher wrapper for [`record_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_record_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RecordPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(FeesPayment, PaymentReceived)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            record_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RecordExpenseCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.recordExpense.record" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "record" }
    fn target_type(&self) -> &'static str { "recordExpense" }
}

/// Dispatcher wrapper for [`record_expense`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_record_expense<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RecordExpenseCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(Expense, ExpenseRecorded)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            record_expense::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ConfigureInvoiceNumberingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.configureInvoiceNumbering.configure" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "configure" }
    fn target_type(&self) -> &'static str { "configureInvoiceNumbering" }
}

/// Dispatcher wrapper for [`configure_invoice_numbering`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_configure_invoice_numbering<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ConfigureInvoiceNumberingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(FeesInvoice, InvoiceNumberingConfigured)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            configure_invoice_numbering::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateIncomeHeadCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.incomeHead.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "incomeHead" }
}

/// Dispatcher wrapper for [`create_income_head`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_income_head<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateIncomeHeadCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealIncomeHead, IncomeHeadCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_income_head::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesAssignDiscount.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesAssignDiscount" }
}

/// Dispatcher wrapper for [`create_fees_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesAssignDiscount, FeesAssignDiscountCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesAssignDiscount.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesAssignDiscount" }
}

/// Dispatcher wrapper for [`read_fees_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesAssignDiscount.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesAssignDiscount" }
}

/// Dispatcher wrapper for [`retire_fees_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesAssignDiscount, FeesAssignDiscountRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesAssign.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesAssign" }
}

/// Dispatcher wrapper for [`create_fees_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesAssign, FeesAssignCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesAssign.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesAssign" }
}

/// Dispatcher wrapper for [`read_fees_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesAssign.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesAssign" }
}

/// Dispatcher wrapper for [`retire_fees_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesAssign, FeesAssignRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesGroupCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesGroup.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesGroup" }
}

/// Dispatcher wrapper for [`create_fm_fees_group`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_group<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesGroupCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFmFeesGroup, FmFeesGroupCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_group::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.invoiceSetting.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "invoiceSetting" }
}

/// Dispatcher wrapper for [`create_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealInvoiceSetting, InvoiceSettingCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateQuestionBankFeeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.questionBankFee.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "questionBankFee" }
}

/// Dispatcher wrapper for [`create_question_bank_fee`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_question_bank_fee<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateQuestionBankFeeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealQuestionBankFee, QuestionBankFeeCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_question_bank_fee::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesSetting.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesSetting" }
}

/// Dispatcher wrapper for [`create_direct_fees_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDirectFeesSetting, DirectFeesSettingCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesCarryForwardLogCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesCarryForwardLog.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesCarryForwardLog" }
}

/// Dispatcher wrapper for [`create_fees_carry_forward_log`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_carry_forward_log<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesCarryForwardLogCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesCarryForwardLog, FeesCarryForwardLogCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_carry_forward_log::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesCarryForwardSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesCarryForwardSetting.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesCarryForwardSetting" }
}

/// Dispatcher wrapper for [`create_fees_carry_forward_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_carry_forward_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesCarryForwardSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesCarryForwardSetting, FeesCarryForwardSettingCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_carry_forward_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesGroupCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesGroup.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesGroup" }
}

/// Dispatcher wrapper for [`read_fm_fees_group`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_group<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesGroupCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_group::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesInvoiceLineNoteCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceLineNote.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceLineNote" }
}

/// Dispatcher wrapper for [`create_fm_fees_invoice_line_note`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_invoice_line_note<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesInvoiceLineNoteCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFmFeesInvoiceLineNote, FmFeesInvoiceLineNoteCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_invoice_line_note::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesTransactionLineNoteCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesTransactionLineNote.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesTransactionLineNote" }
}

/// Dispatcher wrapper for [`create_fm_fees_transaction_line_note`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_transaction_line_note<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesTransactionLineNoteCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(
    RealFmFeesTransactionLineNote,
    FmFeesTransactionLineNoteAdded,
)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_transaction_line_note::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesInstallmentAssignChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallmentAssignChild.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesInstallmentAssignChild" }
}

/// Dispatcher wrapper for [`create_direct_fees_installment_assign_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_installment_assign_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesInstallmentAssignChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(
    RealDirectFeesInstallmentAssignChild,
    DirectFeesInstallmentAssignChildAdded,
)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_installment_assign_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateChartOfAccountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.chartOfAccount.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "chartOfAccount" }
}

/// Dispatcher wrapper for [`create_chart_of_account`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_chart_of_account<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateChartOfAccountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealChartOfAccount, ChartOfAccountCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_chart_of_account::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateSalaryTemplateCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.salaryTemplate.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "salaryTemplate" }
}

/// Dispatcher wrapper for [`create_salary_template`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_salary_template<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateSalaryTemplateCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealSalaryTemplate, SalaryTemplateCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_salary_template::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateBankPaymentSlipAuditCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankPaymentSlipAudit.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "bankPaymentSlipAudit" }
}

/// Dispatcher wrapper for [`create_bank_payment_slip_audit`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_bank_payment_slip_audit<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateBankPaymentSlipAuditCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealBankPaymentSlipAudit, BankPaymentSlipAuditCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_bank_payment_slip_audit::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateBankStatementAttachmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankStatementAttachment.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "bankStatementAttachment" }
}

/// Dispatcher wrapper for [`create_bank_statement_attachment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_bank_statement_attachment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateBankStatementAttachmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(BankStatementAttachment, BankStatementAttachmentCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_bank_statement_attachment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateBankStatementCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankStatement.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "bankStatement" }
}

/// Dispatcher wrapper for [`create_bank_statement`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_bank_statement<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateBankStatementCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealBankStatement, BankStatementCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_bank_statement::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateBankStatementCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankStatement.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "bankStatement" }
}

/// Dispatcher wrapper for [`update_bank_statement`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_bank_statement<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateBankStatementCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankStatementUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_bank_statement::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReverseBankStatementCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.reverseBankStatement.reverse" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "reverse" }
    fn target_type(&self) -> &'static str { "reverseBankStatement" }
}

/// Dispatcher wrapper for [`reverse_bank_statement`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_reverse_bank_statement<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReverseBankStatementCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankStatementReversed>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            reverse_bank_statement::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireBankStatementCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankStatement.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "bankStatement" }
}

/// Dispatcher wrapper for [`retire_bank_statement`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_bank_statement<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireBankStatementCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankStatementRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_bank_statement::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesTypeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesType.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesType" }
}

/// Dispatcher wrapper for [`create_fm_fees_type`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_type<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesTypeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFmFeesType, FmFeesTypeCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_type::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesTypeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesType.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesType" }
}

/// Dispatcher wrapper for [`read_fm_fees_type`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_type<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesTypeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_type::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesTypeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesType.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesType" }
}

/// Dispatcher wrapper for [`retire_fm_fees_type`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_type<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesTypeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesTypeRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_type::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateBankPaymentSlipCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankPaymentSlip.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "bankPaymentSlip" }
}

/// Dispatcher wrapper for [`create_bank_payment_slip`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_bank_payment_slip<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateBankPaymentSlipCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealBankPaymentSlip, BankPaymentSlipCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_bank_payment_slip::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadBankPaymentSlipCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readBankPaymentSlip.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readBankPaymentSlip" }
}

/// Dispatcher wrapper for [`read_bank_payment_slip`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_bank_payment_slip<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadBankPaymentSlipCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_bank_payment_slip::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireBankPaymentSlipCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankPaymentSlip.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "bankPaymentSlip" }
}

/// Dispatcher wrapper for [`retire_bank_payment_slip`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_bank_payment_slip<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireBankPaymentSlipCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankPaymentSlipRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_bank_payment_slip::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesTransaction.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesTransaction" }
}

/// Dispatcher wrapper for [`create_fm_fees_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFmFeesTransaction, FmFeesTransactionCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesTransaction.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesTransaction" }
}

/// Dispatcher wrapper for [`read_fm_fees_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesTransaction.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesTransaction" }
}

/// Dispatcher wrapper for [`retire_fm_fees_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesTransactionRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallment.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesInstallment" }
}

/// Dispatcher wrapper for [`create_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesInstallment, FeesInstallmentCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesInstallment.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesInstallment" }
}

/// Dispatcher wrapper for [`read_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallment.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesInstallment" }
}

/// Dispatcher wrapper for [`retire_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesInstallmentRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesTransactionChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesTransactionChild.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesTransactionChild" }
}

/// Dispatcher wrapper for [`create_fm_fees_transaction_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_transaction_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesTransactionChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFmFeesTransactionChild, FmFeesTransactionChildCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_transaction_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesTransactionChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesTransactionChild.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesTransactionChild" }
}

/// Dispatcher wrapper for [`read_fm_fees_transaction_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_transaction_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesTransactionChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_transaction_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesInvoiceSetting.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesInvoiceSetting" }
}

/// Dispatcher wrapper for [`read_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesInstallmentCreditCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesInstallmentCredit.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesInstallmentCredit" }
}

/// Dispatcher wrapper for [`read_fees_installment_credit`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_installment_credit<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesInstallmentCreditCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_installment_credit::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.transaction.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "transaction" }
}

/// Dispatcher wrapper for [`create_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealTransaction, TransactionCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readTransaction.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readTransaction" }
}

/// Dispatcher wrapper for [`read_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireTransactionCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.transaction.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "transaction" }
}

/// Dispatcher wrapper for [`retire_transaction`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_transaction<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireTransactionCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealTransaction, TransactionRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_transaction::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesInstallmentAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentAssignDiscount.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesInstallmentAssignDiscount" }
}

/// Dispatcher wrapper for [`create_fees_installment_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_installment_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesInstallmentAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(
    RealFeesInstallmentAssignDiscount,
    FeesInstallmentAssignDiscountCreated,
)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_installment_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesInstallmentAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesInstallmentAssignDiscount.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesInstallmentAssignDiscount" }
}

/// Dispatcher wrapper for [`read_fees_installment_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_installment_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesInstallmentAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_installment_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesInstallmentAssignDiscountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentAssignDiscount.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesInstallmentAssignDiscount" }
}

/// Dispatcher wrapper for [`retire_fees_installment_assign_discount`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_installment_assign_discount<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesInstallmentAssignDiscountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(
    RealFeesInstallmentAssignDiscount,
    FeesInstallmentAssignDiscountRetired,
)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_installment_assign_discount::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreatePaymentMethodCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.paymentMethod.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "paymentMethod" }
}

/// Dispatcher wrapper for [`create_payment_method`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_payment_method<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreatePaymentMethodCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealPaymentMethod, PaymentMethodCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_payment_method::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadPaymentMethodCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readPaymentMethod.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readPaymentMethod" }
}

/// Dispatcher wrapper for [`read_payment_method`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_payment_method<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadPaymentMethodCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_payment_method::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetirePaymentMethodCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.paymentMethod.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "paymentMethod" }
}

/// Dispatcher wrapper for [`retire_payment_method`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_payment_method<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetirePaymentMethodCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealPaymentMethod, PaymentMethodRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_payment_method::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentAssign.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesInstallmentAssign" }
}

/// Dispatcher wrapper for [`create_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesInstallmentAssign, FeesInstallmentAssignCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesInstallmentAssign.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesInstallmentAssign" }
}

/// Dispatcher wrapper for [`read_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentAssign.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesInstallmentAssign" }
}

/// Dispatcher wrapper for [`retire_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesInstallmentAssign, FeesInstallmentAssignRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateAmountTransferCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.amountTransfer.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "amountTransfer" }
}

/// Dispatcher wrapper for [`create_amount_transfer`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_amount_transfer<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateAmountTransferCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealAmountTransfer, AmountTransferCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_amount_transfer::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadAmountTransferCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readAmountTransfer.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readAmountTransfer" }
}

/// Dispatcher wrapper for [`read_amount_transfer`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_amount_transfer<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadAmountTransferCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_amount_transfer::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireAmountTransferCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.amountTransfer.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "amountTransfer" }
}

/// Dispatcher wrapper for [`retire_amount_transfer`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_amount_transfer<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireAmountTransferCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealAmountTransfer, AmountTransferRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_amount_transfer::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallment.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesInstallment" }
}

/// Dispatcher wrapper for [`create_direct_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDirectFeesInstallment, DirectFeesInstallmentCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadDirectFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readDirectFeesInstallment.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readDirectFeesInstallment" }
}

/// Dispatcher wrapper for [`read_direct_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_direct_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadDirectFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_direct_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireDirectFeesInstallmentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallment.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "directFeesInstallment" }
}

/// Dispatcher wrapper for [`retire_direct_fees_installment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_direct_fees_installment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireDirectFeesInstallmentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDirectFeesInstallment, DirectFeesInstallmentRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_direct_fees_installment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesCarryForwardCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesCarryForward.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesCarryForward" }
}

/// Dispatcher wrapper for [`create_fees_carry_forward`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_carry_forward<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesCarryForwardCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesCarryForward, FeesCarryForwardCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_carry_forward::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesCarryForwardCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesCarryForward.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesCarryForward" }
}

/// Dispatcher wrapper for [`read_fees_carry_forward`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_carry_forward<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesCarryForwardCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_carry_forward::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesCarryForwardCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesCarryForward.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesCarryForward" }
}

/// Dispatcher wrapper for [`retire_fees_carry_forward`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_carry_forward<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesCarryForwardCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesCarryForward, FeesCarryForwardRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_carry_forward::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesMasterCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesMaster.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesMaster" }
}

/// Dispatcher wrapper for [`create_fees_master`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_master<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesMasterCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesMaster, FeesMasterCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_master::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFeesMasterCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFeesMaster.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFeesMaster" }
}

/// Dispatcher wrapper for [`read_fees_master`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fees_master<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFeesMasterCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fees_master::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesMasterCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesMaster.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesMaster" }
}

/// Dispatcher wrapper for [`retire_fees_master`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_master<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesMasterCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesMaster, FeesMasterRetired)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_master::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDonorCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.donor.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "donor" }
}

/// Dispatcher wrapper for [`create_donor`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_donor<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDonorCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDonor, DonorCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_donor::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadDonorCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readDonor.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readDonor" }
}

/// Dispatcher wrapper for [`read_donor`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_donor<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadDonorCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_donor::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for OpenBankAccountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankAccount.open" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "open" }
    fn target_type(&self) -> &'static str { "bankAccount" }
}

/// Dispatcher wrapper for [`open_bank_account`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_open_bank_account<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: OpenBankAccountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealBankAccount, BankAccountCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            open_bank_account::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateBankAccountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankAccount.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "bankAccount" }
}

/// Dispatcher wrapper for [`update_bank_account`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_bank_account<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateBankAccountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankAccountUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_bank_account::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for DeleteBankAccountCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.bankAccount.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "bankAccount" }
}

/// Dispatcher wrapper for [`retire_bank_account`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_bank_account<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: DeleteBankAccountCommand,
    clock: &C,
    ids: &G
) -> Result<Result<BankAccountRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_bank_account::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesReminderCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesReminder.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesReminder" }
}

/// Dispatcher wrapper for [`create_direct_fees_reminder`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_reminder<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesReminderCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDirectFeesReminder, DirectFeesReminderCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_reminder::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateDirectFeesReminderCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesReminder.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "directFeesReminder" }
}

/// Dispatcher wrapper for [`update_direct_fees_reminder`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_direct_fees_reminder<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateDirectFeesReminderCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesReminderUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_direct_fees_reminder::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for DeleteDirectFeesReminderCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesReminder.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "directFeesReminder" }
}

/// Dispatcher wrapper for [`retire_direct_fees_reminder`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_direct_fees_reminder<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: DeleteDirectFeesReminderCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesReminderRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_direct_fees_reminder::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateExpenseHeadCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseHead.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "expenseHead" }
}

/// Dispatcher wrapper for [`create_expense_head`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_expense_head<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateExpenseHeadCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealExpenseHead, ExpenseHeadCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_expense_head::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateExpenseHeadCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseHead.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "expenseHead" }
}

/// Dispatcher wrapper for [`update_expense_head`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_expense_head<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateExpenseHeadCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ExpenseHeadUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_expense_head::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for DeleteExpenseHeadCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.expenseHead.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "expenseHead" }
}

/// Dispatcher wrapper for [`retire_expense_head`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_expense_head<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: DeleteExpenseHeadCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ExpenseHeadRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_expense_head::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesGroupCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesGroup.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesGroup" }
}

/// Dispatcher wrapper for [`create_fees_group`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_group<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesGroupCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesGroup, FeesGroupCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_group::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateFeesGroupCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesGroup.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "feesGroup" }
}

/// Dispatcher wrapper for [`update_fees_group`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_fees_group<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateFeesGroupCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesGroupUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_fees_group::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for DeleteFeesGroupCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesGroup.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesGroup" }
}

/// Dispatcher wrapper for [`retire_fees_group`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_group<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: DeleteFeesGroupCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesGroupRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_group::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for BlockLoginForDueFeesCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.blockLoginForDueFe.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "blockLoginForDueFe" }
}

/// Dispatcher wrapper for [`create_due_fees_login_prevent`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_due_fees_login_prevent<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: BlockLoginForDueFeesCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealDueFeesLoginPrevent, DueFeesLoginPreventCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_due_fees_login_prevent::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UnblockLoginForDueFeesCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.unblockLoginForDueFe.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "unblockLoginForDueFe" }
}

/// Dispatcher wrapper for [`update_due_fees_login_prevent`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_due_fees_login_prevent<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UnblockLoginForDueFeesCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DueFeesLoginPreventUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_due_fees_login_prevent::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UnblockLoginForDueFeesCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.unblockLoginForDueFe.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "unblockLoginForDueFe" }
}

/// Dispatcher wrapper for [`retire_due_fees_login_prevent`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_due_fees_login_prevent<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UnblockLoginForDueFeesCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DueFeesLoginPreventRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_due_fees_login_prevent::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UnblockLoginForDueFeesCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.unblockLoginForDueFe.prune" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "prune" }
    fn target_type(&self) -> &'static str { "unblockLoginForDueFe" }
}

/// Dispatcher wrapper for [`prune_due_fees_login_prevent`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_prune_due_fees_login_prevent<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UnblockLoginForDueFeesCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DueFeesLoginPreventPruned>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            prune_due_fees_login_prevent::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInvoiceSetting.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesInvoiceSetting" }
}

/// Dispatcher wrapper for [`create_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesInvoiceSetting, FeesInvoiceSettingCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInvoiceSetting.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "feesInvoiceSetting" }
}

/// Dispatcher wrapper for [`update_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesInvoiceSettingUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for DeleteFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInvoiceSetting.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesInvoiceSetting" }
}

/// Dispatcher wrapper for [`retire_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: DeleteFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesInvoiceSettingRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFeesInstallmentCreditCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentCredit.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "feesInstallmentCredit" }
}

/// Dispatcher wrapper for [`create_fees_installment_credit`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fees_installment_credit<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFeesInstallmentCreditCommand,
    clock: &C,
    ids: &G
) -> Result<Result<(RealFeesInstallmentCredit, FeesInstallmentCreditCreated)>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fees_installment_credit::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFeesInstallmentCreditCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.feesInstallmentCredit.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "feesInstallmentCredit" }
}

/// Dispatcher wrapper for [`retire_fees_installment_credit`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fees_installment_credit<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFeesInstallmentCreditCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FeesInstallmentCreditRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fees_installment_credit::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceSetting.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceSetting" }
}

/// Dispatcher wrapper for [`create_fm_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceSettingCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesInvoiceSetting.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesInvoiceSetting" }
}

/// Dispatcher wrapper for [`read_fm_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for UpdateFmFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceSetting.update" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "update" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceSetting" }
}

/// Dispatcher wrapper for [`update_fm_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_update_fm_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: UpdateFmFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceSettingUpdated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            update_fm_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesInvoiceSettingCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceSetting.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceSetting" }
}

/// Dispatcher wrapper for [`retire_fm_fees_invoice_setting`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_invoice_setting<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesInvoiceSettingCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceSettingRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_invoice_setting::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesWeaverCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesWeaver.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesWeaver" }
}

/// Dispatcher wrapper for [`create_fm_fees_weaver`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_weaver<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesWeaverCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesWeaverCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_weaver::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesWeaverCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesWeaver.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesWeaver" }
}

/// Dispatcher wrapper for [`read_fm_fees_weaver`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_weaver<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesWeaverCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_weaver::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesWeaverCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesWeaver.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesWeaver" }
}

/// Dispatcher wrapper for [`retire_fm_fees_weaver`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_weaver<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesWeaverCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesWeaverRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_weaver::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesInstallmentChildPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallmentChildPayment.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesInstallmentChildPayment" }
}

/// Dispatcher wrapper for [`create_direct_fees_installment_child_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_installment_child_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesInstallmentChildPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesInstallmentChildPaymentCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_installment_child_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadDirectFeesInstallmentChildPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readDirectFeesInstallmentChildPayment.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readDirectFeesInstallmentChildPayment" }
}

/// Dispatcher wrapper for [`read_direct_fees_installment_child_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_direct_fees_installment_child_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadDirectFeesInstallmentChildPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_direct_fees_installment_child_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireDirectFeesInstallmentChildPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallmentChildPayment.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "directFeesInstallmentChildPayment" }
}

/// Dispatcher wrapper for [`retire_direct_fees_installment_child_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_direct_fees_installment_child_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireDirectFeesInstallmentChildPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesInstallmentChildPaymentRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_direct_fees_installment_child_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateIncomeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.income.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "income" }
}

/// Dispatcher wrapper for [`create_income`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_income<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateIncomeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<IncomeCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_income::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadIncomeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readIncome.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readIncome" }
}

/// Dispatcher wrapper for [`read_income`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_income<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadIncomeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_income::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireIncomeCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.income.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "income" }
}

/// Dispatcher wrapper for [`retire_income`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_income<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireIncomeCommand,
    clock: &C,
    ids: &G
) -> Result<Result<IncomeRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_income::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateInventoryPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.inventoryPayment.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "inventoryPayment" }
}

/// Dispatcher wrapper for [`create_inventory_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_inventory_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateInventoryPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<InventoryPaymentCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_inventory_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadInventoryPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readInventoryPayment.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readInventoryPayment" }
}

/// Dispatcher wrapper for [`read_inventory_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_inventory_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadInventoryPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_inventory_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireInventoryPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.inventoryPayment.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "inventoryPayment" }
}

/// Dispatcher wrapper for [`retire_inventory_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_inventory_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireInventoryPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<Result<InventoryPaymentRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_inventory_payment::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateProductPurchaseCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.productPurchase.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "productPurchase" }
}

/// Dispatcher wrapper for [`create_product_purchase`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_product_purchase<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateProductPurchaseCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ProductPurchaseCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_product_purchase::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadProductPurchaseCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readProductPurchase.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readProductPurchase" }
}

/// Dispatcher wrapper for [`read_product_purchase`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_product_purchase<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadProductPurchaseCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_product_purchase::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireProductPurchaseCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.productPurchase.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "productPurchase" }
}

/// Dispatcher wrapper for [`retire_product_purchase`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_product_purchase<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireProductPurchaseCommand,
    clock: &C,
    ids: &G
) -> Result<Result<ProductPurchaseRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_product_purchase::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesInvoiceCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoice.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesInvoice" }
}

/// Dispatcher wrapper for [`create_fm_fees_invoice`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_invoice<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesInvoiceCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_invoice::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesInvoiceCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesInvoice.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesInvoice" }
}

/// Dispatcher wrapper for [`read_fm_fees_invoice`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_invoice<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesInvoiceCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_invoice::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesInvoiceCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoice.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesInvoice" }
}

/// Dispatcher wrapper for [`retire_fm_fees_invoice`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_invoice<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesInvoiceCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_invoice::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateFmFeesInvoiceChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceChild.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceChild" }
}

/// Dispatcher wrapper for [`create_fm_fees_invoice_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_fm_fees_invoice_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateFmFeesInvoiceChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceChildCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_fm_fees_invoice_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadFmFeesInvoiceChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readFmFeesInvoiceChild.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readFmFeesInvoiceChild" }
}

/// Dispatcher wrapper for [`read_fm_fees_invoice_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_fm_fees_invoice_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadFmFeesInvoiceChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_fm_fees_invoice_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireFmFeesInvoiceChildCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.fmFeesInvoiceChild.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "fmFeesInvoiceChild" }
}

/// Dispatcher wrapper for [`retire_fm_fees_invoice_child`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_fm_fees_invoice_child<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireFmFeesInvoiceChildCommand,
    clock: &C,
    ids: &G
) -> Result<Result<FmFeesInvoiceChildRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_fm_fees_invoice_child::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for CreateDirectFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallmentAssign.create" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "create" }
    fn target_type(&self) -> &'static str { "directFeesInstallmentAssign" }
}

/// Dispatcher wrapper for [`create_direct_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_create_direct_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: CreateDirectFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesInstallmentAssignCreated>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            create_direct_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ReadDirectFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.readDirectFeesInstallmentAssign.read" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "read" }
    fn target_type(&self) -> &'static str { "readDirectFeesInstallmentAssign" }
}

/// Dispatcher wrapper for [`read_direct_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_read_direct_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ReadDirectFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<()>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            read_direct_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RetireDirectFeesInstallmentAssignCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.directFeesInstallmentAssign.retire" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "retire" }
    fn target_type(&self) -> &'static str { "directFeesInstallmentAssign" }
}

/// Dispatcher wrapper for [`retire_direct_fees_installment_assign`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_retire_direct_fees_installment_assign<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RetireDirectFeesInstallmentAssignCommand,
    clock: &C,
    ids: &G
) -> Result<Result<DirectFeesInstallmentAssignRetired>
where
    C: Clock + ?Sized,
    G: IdGenerator + ?Sized,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            retire_direct_fees_installment_assign::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for ConfigurePaymentGatewayCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.configurePaymentGateway.configure" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "configure" }
    fn target_type(&self) -> &'static str { "configurePaymentGateway" }
}

/// Dispatcher wrapper for [`configure_payment_gateway`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_configure_payment_gateway<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: ConfigurePaymentGatewayCommand,
    clock: &C,
    ids: &G
) -> Result<educore_core::error::Result<(RealPaymentGatewaySetting, PaymentGatewayConfigured)>
where
    C: educore_core::clock::Clock,
    G: Fn() -> educore_core::ids::EventId,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            configure_payment_gateway::<C, G>(cmd)
        })
        .await
}


impl educore_dispatcher::CommandBounds for RecordPayrollPaymentCommand {
    fn tenant(&self) -> &TenantContext { &self.tenant }
    fn command_type(&self) -> &'static str { "<domain>.recordPayrollPayment.record" }
    fn idempotency_key(&self) -> Option<IdempotencyKey> { None }
    fn action(&self) -> &'static str { "record" }
    fn target_type(&self) -> &'static str { "recordPayrollPayment" }
}

/// Dispatcher wrapper for [`record_payroll_payment`].
///
/// Mirrors the Wave 192 template: clones `cmd` inside the closure
/// to satisfy the borrow checker, then runs the full
/// `CommandDispatcher::dispatch` pipeline (RBAC → txn →
/// idempotency → service → outbox → audit → idempotency record
/// → bus publish).
pub async fn dispatch_record_payroll_payment<C, G>(
    dispatcher: &CommandDispatcher,
    cmd: RecordPayrollPaymentCommand,
    clock: &C,
    ids: &G
) -> Result<educore_core::error::Result<(RealPayrollPayment, PayrollPaymentRecorded)>
where
    C: educore_core::clock::Clock,
    G: Fn() -> educore_core::ids::EventId,>
where
    C: Clock + ?Sized + Send + Sync,
    G: IdGenerator + ?Sized + Send + Sync,
{
    use educore_dispatcher::CommandBounds as _;
    dispatcher
        .dispatch(&cmd, &["<capability>"], || async {
            record_payroll_payment::<C, G>(cmd)
        })
        .await
}

